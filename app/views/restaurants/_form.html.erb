<%= form_with(model: restaurant) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
        <% restaurant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :restaurant_name %>
    <%= form.text_field :restaurant_name %>
  </div>

  <div class="field">
    <%= form.label :restaurant_contactNo %>
    <%= form.number_field :restaurant_contactNo %>
  </div>

  <div class="field">
    <%= form.label :restaurant_status %>
    <%= form.text_field :restaurant_status %>
  </div>

  <div class="field">
    <%= form.label :restaurant_timings %>
    <%= form.datetime_select :restaurant_timings %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
